// const typeList = ['man', 'women', 'child'];

// const addKind = (type) => {
//   return `${type}-kind`;
// }

// console.log(typeList);
// let newTypes = typeList.map(addKind)

// console.log(newTypes);

// const displayInfo = (value) => { // arrow func
//   console.log("Area is ", value);
// }

// function displayFunc(val, type){ // function keyword
//   console.log(`Area of ${type} is `, val);
// }

// const calAreaRect = ( l, b, displayData) => { // Accepts a function to display data
//   const area = l * b;
//   console.log(displayData);
// }

// calAreaRect(10, 5,  (value) => {
//   console.log(value);
// });

// const sampleFunc = () => console.log("Line 32")
// console.log("Line 30");
// console.log("Line 31");
// setTimeout(sampleFunc, 3000);
// console.log("Line 33");


// setInterval(() => {
  
// }, 60000);

// Set Text to Display
const showText = () => {

}

// Calculate Value and return to display
const calculateValue = (operand1, operand2, operator) => {
  switch(operator) {
    case "+":
      return operand1 + operand2;
  }
}

// Clearing Display
const clearDisplay = () => {
  
}


function f1() {
  // Some value which will be passed to below function
  var result = null;
  f2(result);
}

function f2(res) {
  // Some value which will be passed to below function
  f3(res);
}

function f3(res) {
  // Some value which will be passed to below function
  f4(res);
}

function f4(res) {
  
}
